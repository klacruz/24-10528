La tarea consiste en:

1. implementar sfib en Zilly+ (5 puntos)

2. (bono: 1 punto adicional) ¿Notan una relación entre sfib con fibo?
De ser así, consiga la fórmula e implemente la función zfib, tal the zfib(n) = sfib(n), para todo n.
La implementación de zfib no debe ser recursiva, sólo depender directamente de fibo.

3. (bono: 2 puntos adicionales) ¿Es zfib más rápido que sfib?
Conteste SI o NO y explique porqué, de manera clara, rigurosa, y en pocas líneas, ya que no hacen falta muchas.

Importante: deben tener la definicion de fibo (tal y como se muestra arriba) en el mismo archivo de sfib

1. Z => Z sfib := fn(Z n) => Z -> if(n < 1, 0, sfib(n - 1) + fibo(n));
2. ⁠La suma acumulada de la serie de Fibonacci desde el índice 0 hasta la posición n es exactamente igual al valor del término ubicado en la posición n+2 de la secuencia, restándole una unidad.    Z => Z zfib := fn(Z n) => Z -> fibo(n + 2) - 1;
3. No. Aunque zfib elimina la recursión de la sumatoria, delega todo el trabajo bruto a fibo(n+2). Esta única llamada desencadena una gran cantidad de sub-llamadas redundantes, cuyo costo de procesamiento equivale a calcular y sumar cada uno de los términos individuales como lo hace sfib.
