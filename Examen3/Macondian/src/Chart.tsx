///
/// Chart
///

interface Props {
  data: number[]; // ??? no necesariamente tan simple: depende de lo que hagan
};

const Chart = (props: Props) => {
  return (
    <h2> Aqui va "El Chart" </h2>
  );
};

export default Chart;
